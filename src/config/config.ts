export const URLSearchParamsKeyword = {
  SEARCH: "search",
  PAGE: "page",
  SIZE: "size"
} as const;

export const defaultParam = {
  size: 10
};

export const urlParamsPrefix = {
  FILTER: "filter",
  SORT: "sort"
}
